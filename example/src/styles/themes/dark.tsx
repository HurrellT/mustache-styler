import { StyleSheet } from 'react-native';
import lightColors from '../layout/colors';

const darkTheme = {
  buttons: StyleSheet.create({
    primary: {
      color: lightColors.blue600,
    },
  }),
};

export default darkTheme;
